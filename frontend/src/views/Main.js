import React from "react";
import "../App.css";

const Main = () => {
  return (
    <div class="text-bg-info p-2">
      {/*
      <h3> 프로젝트 <em class = "red-text">내부통제(투입 인력관리) </em>Main 입니다.</h3>
      */}
      <p class="text-center fs-3 fw-bold">프로젝트 <em class = "red-text">내부통제(투입 인력관리) </em>시스템 입니다.</p>
    </div>
  );
};

export default Main;
